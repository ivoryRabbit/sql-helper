[pytest]
asyncio_mode = auto
testpaths = test
pythonpath = src

# Show locals on failure, verbose by default
addopts = -v --tb=short

# Silence noisy third-party loggers during tests
log_cli = true
log_cli_level = WARNING
log_level = WARNING
